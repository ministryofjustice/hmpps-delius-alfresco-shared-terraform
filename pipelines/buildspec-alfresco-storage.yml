---
version: 0.2

phases:
  pre_build:
    commands:
      - make get_configs
  build:
    commands:
      - export HMPPS_BUILD_WORK_DIR=${CODEBUILD_SRC_DIR}
      - make ${ACTION_TYPE} component=efs
      - make ${ACTION_TYPE} component=elasticache-memcached
      - make ${ACTION_TYPE} component=elk-migration
